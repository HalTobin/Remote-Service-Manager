name: data

environment:
  sdk: ^3.9.2
  flutter:

dependencies:
  flutter:
    sdk: flutter

  path_provider:
  flutter_secure_storage:
  dartssh2:
  drift:
  drift_flutter:
  biometric_storage:
  json_serializable:
  collection:

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

resolution:
  workspace

flutter: